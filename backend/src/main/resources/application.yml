spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finwell
    username: postgres
    password: "1914"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080